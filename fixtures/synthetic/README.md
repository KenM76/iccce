# fixtures/synthetic

Profiles authored byte-by-byte by this project's own generator
(`tools/gen-profiles`, built during Pass 2). Unrestricted, category (a)
per `docs/LEGAL.md` §3 — **prefer these for everything**.

Nothing here yet: the generator does not exist. When it does, every
fixture in this directory is reproducible from a generator invocation
recorded alongside it — a synthetic fixture that cannot be regenerated
is just a binary blob with better branding.

{{!-- iccce — cargo-about template for THIRD_PARTY_LICENSES.md.

    Emits Markdown, not cargo-about's default HTML, because docs/LEGAL.md
    §1 names the artefact `THIRD_PARTY_LICENSES.md` and the rest of the
    project's documentation is Markdown.

    Regenerate with:
      cargo about generate --workspace about.hbs -o THIRD_PARTY_LICENSES.md

    ★ READ THIS BEFORE EDITING THE PROSE BELOW. cargo-about reports the
    WHOLE resolved graph, which includes the workspace's own crates — it
    has no notion of "first party". So as of 2026-08-17, with no
    third-party dependencies at all, the only entries it emits are
    iccce's own workspace crates under the project's MIT licence.

    ★ AND DO NOT WRITE THE NUMBER OF THEM. This prose said "four" from
    2026-08-12 until 2026-08-17, during which `iccce-measure` was added
    as a fifth workspace member and every sentence asserting "four"
    became false — in a GENERATED file, where the count is not the
    generator's output but the template author's memory of the
    workspace. Regeneration cannot fix a hardcoded count, which is
    precisely why the banner above already forbids asserting a count of
    dependencies; the same reasoning was simply never applied to a count
    of OUR OWN crates. The prose below now states the `iccce-` PREFIX
    RULE instead, which survives the sixth crate.

    That makes the intro prose delicate: anything it asserts must stay
    true both now (zero third-party entries) and later (some). Hence it
    tells the reader HOW TO READ the listing — `iccce-*` is us, anything
    else is a dependency — rather than asserting a count that the next
    dependency would falsify without anyone noticing. Do not "simplify"
    it back into "this file lists our third-party dependencies"; that
    sentence is false today.

    The `{{#each licenses}}…{{else}}…{{/each}}` inverse block is a guard
    for the genuinely-empty case (e.g. if the config is ever changed to
    exclude workspace members): an empty section header would otherwise
    read as "nobody filled this in". --}}
# Licences

<!-- ============================================================= -->
<!-- GENERATED FILE — DO NOT EDIT BY HAND.                         -->
<!--                                                               -->
<!-- Produced by cargo-about from `about.toml` + `about.hbs`.      -->
<!-- docs/LEGAL.md §1: this file "is generated by cargo-about,     -->
<!-- never hand-written." Edit the template, not the output.       -->
<!--                                                               -->
<!--   cargo about generate --workspace about.hbs \                -->
<!--       -o THIRD_PARTY_LICENSES.md                              -->
<!-- ============================================================= -->

Every crate in the `iccce` workspace's **resolved dependency graph**,
grouped by licence, with the full text of each licence. Regenerated from
the lockfile by `cargo-about`; not a summary anyone maintains by hand.

**How to read the listing.** cargo-about reports the whole graph and has
no notion of "first party", so this file names iccce's own crates too.
**Every crate listed below whose name begins with `iccce-` is this
project** — those carry the project's own MIT licence (`LICENSE`, MIT,
Copyright (c) 2026 Ken Mantle), and the workspace's membership list in
the root `Cargo.toml` is their authoritative enumeration. **Every crate
whose name does not begin with `iccce-` is a third-party dependency**,
and its licence text is reproduced here because iccce redistributes it.
The rule is stated as a prefix rather than as a list of names so that
adding a workspace crate cannot silently falsify this paragraph.

**Scope, stated because a generated file cannot state its own limits.**
This manifest covers Cargo dependencies of the `iccce-` workspace
crates and nothing else. In particular it does **not** cover **lcms2**, which is not
a dependency of any of them: lcms2 is an out-of-tree differential-test
oracle driven as a separate process from `tools/difftest`, which is
deliberately not a workspace member, and whose source is git-ignored so
it never enters this repository. The licence analysis for lcms2 —
including `docs/LEGAL.md` §4.2's finding that lcms2 is **not** uniformly
MIT, its `fast_float` and `threaded` plugins being GPL-3.0 and not built
— is in `docs/LEGAL.md` §4, and a dependency-licence generator cannot see
any of it. That is why §4 is written out longhand and why this file is
not the whole licence story.

{{#each licenses}}
{{else}}
## Nothing to list

The generator resolved **no crates at all** into this manifest.

That is only the correct output if the configuration deliberately
excludes the workspace's own members; otherwise it means the generator
did not see the graph it was pointed at, and the empty result is a tool
failure rather than a finding. Check `about.toml` before treating this
section as evidence of anything.
{{/each}}
{{#each licenses}}
## {{name}}

Used by:

{{#each used_by}}
- [{{crate.name}} {{crate.version}}]({{#if crate.repository}}{{crate.repository}}{{else}}https://crates.io/crates/{{crate.name}}{{/if}})
{{/each}}

```text
{{{text}}}
```

{{/each}}
